import React from 'react';
import { useQuery } from 'urql';
import gql from 'graphql-tag';



const LinkCard = props => {
console.log(props);

    return(
        <div>
           <div>hi this is where the individual card for this link would show up</div> 
        </div>
    );
};

export default LinkCard;